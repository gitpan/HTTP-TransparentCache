HTTP-TransparentCache has been renamed to HTTP::Cache::Transparent by request
from modules -at- perl -dot- org. Please use that module instead.
It can be downloaded from CPAN at 

  http://search.cpan.org/dist/HTTP-Cache-Transparent
 

